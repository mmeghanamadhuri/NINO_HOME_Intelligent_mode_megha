dependencies:
  espressif/esp_hosted:
    version: "*"
    rules:
      - if: "target in [esp32p4]"
  espressif/esp_wifi_remote:
    version: "*"
    rules:
      - if: "target in [esp32p4]"
  espressif/usb:
    rules:
      - if: "idf_version >=5.4"
    version: "*"
  espressif/usb_host_uvc:
    version: "*"
  idf: ">=5.5"
